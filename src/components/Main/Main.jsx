// import Container from "../Container/Container"

export default function Main({children}) {
    return (
      <main className="main">
        {children}
        {/* <Container /> */}
      </main>
    )
}